{
  "name": "vstupni-test",
  "version": "1.0.0",
  "description": "Search page with Cypress E2E tests",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.0.0"
  }
}
